# 🏋️ FitQR 개발일지 (v2)

> 각 단계에서 **무슨 생각으로 / 어떻게 / 왜** 그렇게 구현했는지 기록하는 문서.
> ⚠️ 이 문서는 **실제 코드(`app.py` · `templates/` · `static/`) 기준**이다. 코드와 다르면 코드가 1순위.
> (구버전 v1 — 건강진단·식단추천·질환규칙·가동범위 카운트 — 은 **전면 폐기**되었고 현재 코드에 없음.)

---

## 📐 전체 설계 원칙

1. **교수님 PDF(11회차) 패턴을 그대로 따른다.**
   - `get_db()` 로 DB 연결, `init_db()` 로 테이블 생성 + 시드
   - `session` 으로 로그인 상태 관리, `role` 로 관리자 구분
   - `render_template()` + Jinja2 로 화면 분리, 파라미터 바인딩(`?`)으로 SQL 인젝션 방지

2. **단일 파일(`app.py`)로 끝까지 완성**
   - DB 스키마 → 시드 → 라우트 → 실행이 한 파일에 순서대로. 채점/시연 시 흐름 추적이 쉬움.

3. **항상 "돌아가는 상태" 유지 + 오류 방지 최우선**
   - 숫자 입력은 빈 값이 와도 기본값으로 안전 변환
   - 모든 조회에 소유권(`user_id`) 검증 → 남의 데이터/없는 자원 접근 차단

---

## 🔄 v1 → v2 전면 재설계 (왜 갈아엎었나)

초기 v1은 "QR + 건강진단 + 식단추천 + 질환규칙 + 쿠폰"으로 기능이 넓고 산만했다.
**핵심(머신 QR → 운동 → 자동 카운트 → 기록 → 보상)에 집중**하기 위해 v2로 전부 새로 작성했다.

| 구분 | v1 (폐기) | v2 (현재 코드) |
|------|-----------|----------------|
| 테이블 | 12~13개 (health_profile, diet_plan, condition_rule …) | **11개** (운동 트래킹 중심) |
| 카운트 | 가동범위 슬라이더(min_valid_rom) | **[+1 Rep] + 휴식 8초 자동 세트확정** |
| 추천 | 규칙기반 식단/재활 추천 | **30일 루틴 자동 생성** |
| 화면 | 대시보드 + 기능 메뉴 | **하단 5탭** (루틴/커스텀/운동/리포트/내 정보) |
| 기록 | workout_session → set_log (2계층) | **workout → machine_use → set_log (3계층)** |

---

## ✅ 0단계 — 프로젝트 토대 + DB 설계 (11테이블)

### 🤔 무슨 생각으로
모든 기능이 DB 위에서 도므로 **테이블 구조를 먼저 확정**했다.
운동 기록을 "운동 1회 → 그 안의 종목 사용들 → 각 종목의 세트들"이라는 **3계층**으로 모델링한 게 핵심.

### 🗄️ DB 테이블 11개
| # | 테이블 | 역할 |
|---|--------|------|
| 1 | users | 회원(신체정보·목표·빈도·시작요일·1RM·role) |
| 2 | exercise | 운동 종목 마스터(머신/비머신 통합, `is_machine`) |
| 3 | routine | 회원당 30일 루틴 헤더(status·reward_given) |
| 4 | routine_day | 1~30일 각 날(요일·부위·is_unlocked·is_completed) |
| 5 | routine_day_item | 그날의 운동 종목(target_sets·reps·seq) |
| 6 | workout | "운동 종료" 1단위(source·duration·total_volume) |
| 7 | machine_use | "사용 종료" 1단위(한 종목 사용·weight) |
| 8 | set_log | 세트 1개(set_number·reps·weight) |
| 9 | custom_routine / custom_routine_item | 사용자 커스텀 루틴 |
| 10 | weight_log | 내 체중 기록(리포트 차트용) |
| 11 | coupons / user_coupon | 쿠폰 마스터 + 지급내역(N:M) |

### 🔑 핵심 코드 — DB 연결 (FK 활성화)
```python
def get_db():
    conn = sqlite3.connect(DB_PATH)
    cursor = conn.cursor()
    cursor.execute('PRAGMA foreign_keys = ON')   # 외래키 제약 켜기
    return conn, cursor
```

### 🔑 핵심 코드 — 시드 (중복 방지)
```python
cursor.execute('SELECT COUNT(*) FROM exercise')
if cursor.fetchone()[0] == 0:
    seed_exercises(cursor)     # 부위별 머신 5종+ & 비머신 대표 종목
```
> 시드: 관리자 `admin/1234`, 운동 종목(가슴·등·어깨·팔·복근·엉덩이·다리·전신 머신 + 바벨스쿼트·데드리프트·푸시업·런지·플랭크 등 비머신), 쿠폰 5종(마이프로틴·GNC·나이키·언더아머·데카트론).

### 🔑 핵심 코드 — `is_machine` 한 컬럼으로 머신/거울 통합
```sql
is_machine INTEGER DEFAULT 1   -- 1=머신(QR 스캔) / 0=비머신(전신거울 카메라)
```
> 머신/비머신을 **한 테이블**로 통합 → 루틴 생성·카탈로그·운동화면 로직을 단순화.

---

## ✅ 1단계 — 회원가입 / 로그인 / 로그아웃

### 🤔 무슨 생각으로
모든 기능이 "로그인한 회원"(`user_id`) 기준으로 동작하므로 인증을 먼저 세웠다.
v2에서는 **가입과 동시에 30일 루틴을 만들고 자동 로그인**시켜 곧바로 홈(루틴 탭)을 보여준다.

### ⚙️ 동작 방식
```
[회원가입] /register/
   POST → 아이디 중복확인 → users INSERT
        → 첫 체중 weight_log 기록
        → create_routine_for_user() 로 30일 루틴 생성
        → 자동 로그인(session 설정) → '/'

[로그인]  /login/  → session.clear() 후 재설정 → '/'
[로그아웃] /logout/ → session.clear() → /login/
```

### 🔑 핵심 코드 — 가입 시 루틴 자동 생성 + 자동 로그인
```python
cursor.execute('SELECT * FROM users WHERE id=?', (uid,))
create_routine_for_user(cursor, cursor.fetchone())   # 30일 루틴
...
session['user_id'] = uid; session['name'] = name; session['role'] = 'user'
return redirect('/')
```

### 🔑 핵심 코드 — 유령 세션 방지 (`require_login`)
```python
def require_login():
    if 'user_id' not in session:
        return False
    # 세션 user_id가 실제 DB에 존재하는지까지 확인
    cursor.execute('SELECT id FROM users WHERE id=?', (session['user_id'],))
    exists = cursor.fetchone() is not None
    if not exists:
        session.clear()   # DB 리셋 후 남은 쿠키 정리
    return exists
```
> **왜?** 개발 중 DB를 지워도 브라우저 쿠키가 남아 "있지도 않은 유저"로 로그인된 것처럼 보이는 버그를 막는다.

### 🔑 핵심 코드 — 계정 전환 캐시 버그 차단
```python
@app.after_request
def add_no_cache(resp):
    resp.headers['Cache-Control'] = 'no-store, no-cache, must-revalidate, max-age=0'
    ...
```
> 계정을 바꿨는데 이전 사용자 화면이 캐시로 보이는 현상 방지.

---

## ✅ 2단계 — 30일 루틴 자동 생성 (개인화 엔진)

### 🤔 무슨 생각으로
가입 정보(빈도·집중부위·레벨·목표)를 받아 **30일짜리 운동 계획을 자동 생성**한다.
AI 없이 **상수 매핑 + 조건 분기**로 구현해 안정적이고 설명하기 쉽다.

### ⚙️ 입력 → 생성 규칙
```
빈도(frequency) → 주간 운동 요일 오프셋   FREQ_OFFSETS  (3회→[0,2,4], 5회→[0,1,2,3,4])
집중부위(focus) → 부위 순환 리스트        build_part_cycle (선택 부위를 앞에 배치)
레벨(level)     → 세트 수·종목 수         SETS/COUNT_BY_LEVEL (초3·중4·상5)
목표(goal)      → 목표 reps              REPS_BY_GOAL (강해지기6·근육량12·체중감량15)
```

### 🔑 핵심 코드 — 부위 순환 (집중 부위 우선)
```python
def build_part_cycle(focus_part):
    base = ['가슴','등','다리','어깨','팔','엉덩이','복근']
    focus = [x for x in focus_part.split(',') if x in base]
    return focus + [p for p in base if p not in focus] if focus else base
```
> 집중 부위(복수 선택 가능)를 앞쪽에 배치하고 나머지를 뒤에 붙여 **다양성 유지**.

### 🔑 핵심 코드 — 그날 종목 선택 (머신 우선 + 셔플)
```python
cursor.execute('''SELECT id FROM exercise WHERE body_part=?
                  ORDER BY is_machine DESC, id''', (body_part,))
ex_ids = [r[0] for r in cursor.fetchall()]
if len(ex_ids) < count:                       # 부족하면 전신으로 보충
    cursor.execute("SELECT id FROM exercise WHERE body_part='전신' ORDER BY id")
    ex_ids += [r[0] for r in cursor.fetchall()]
random.shuffle(ex_ids)
chosen = ex_ids[:count]
```
> 1일차만 `is_unlocked=1` → 앞 날을 끝내야 다음 날이 열리는 **해금 구조**.

---

## ✅ 3단계 — 머신 QR + 웹캠 스캔 ⭐ (이 프로젝트의 심장)

### 🤔 무슨 생각으로
지쿠처럼 **QR을 찍고 들어가면 그 머신의 운동 정보가 뜨는** 흐름을 웹으로 구현.
실제 QR 스캐너 대신 **노트북 웹캠**으로 시연한다.

### ⚙️ 동작 방식
```
[QR 이미지]   /machine/<id>/qr.png   머신 URL을 PNG QR로 즉석 생성
[QR 모아보기] /qrcodes/              머신 QR을 한 화면에(부위 필터) → 띄워놓고 스캔
[웹캠 스캔]   /scan/                 카메라 ON → QR 인식 → /machine/<id>/?wid=... 이동
[머신 정보]   /machine/<id>/         준비/수행/팁 표시 → [이 머신으로 운동]
```

### 🔑 핵심 코드 ① — QR 이미지 동적 생성 (도메인 자동)
```python
@app.route('/machine/<int:eid>/qr.png')
def machine_qr(eid):
    url = url_for('machine', eid=eid, _external=True)   # 현재 호스트 자동 감지
    img = qrcode.make(url)
    buf = io.BytesIO(); img.save(buf, 'PNG'); buf.seek(0)
    return send_file(buf, mimetype='image/png')
```
> `_external=True` 가 접속 주소를 자동 감지 → 로컬/배포 어디서든 맞는 QR 생성. (`qrcode` 미설치면 404로 graceful)

### 🔑 핵심 코드 ② — 웹캠 직접 인식 (scan.html) ★ 최근 개선
**문제**: 기존 `Html5QrcodeScanner`(고수준 위젯)는 "이미지 파일 스캔" 탭이 함께 뜨고,
권한 버튼을 눌러야 카메라가 켜져서 "웹캠 연동이 안 되는 것처럼" 보였다.

**해결**: `Html5Qrcode`(저수준 API)로 교체 → **진입 시 후면 카메라 자동 시작, 파일 업로드 제거.**
```javascript
var html5Qr = new Html5Qrcode('reader');
html5Qr.start({ facingMode:'environment' }, config, onScanSuccess, ()=>{})
  .catch(function(){                         // 실패 시 카메라 목록으로 재시도
    Html5Qrcode.getCameras().then(cams => {
      html5Qr.start(cams[cams.length-1].id, config, onScanSuccess, ()=>{});
    });
  });

function onScanSuccess(text){
    var m = text.match(/\/machine\/(\d+)\/?/);     // URL에서 머신번호 추출
    var url = '/machine/' + m[1] + '/' + (WID ? '?wid='+WID : '');
    html5Qr.stop().then(()=> location.href = url); // 현재 origin 기준 이동
}
```
> 인식 성공 시 `scanned` 플래그로 중복 이동 방지, 실패하면 **에러 메시지 + [다시 시도] 버튼** 표시.

### ⚠️ 카메라 동작 조건 (꼭 기억)
브라우저 카메라(`getUserMedia`)는 **보안 컨텍스트**에서만 동작.
- ✅ `localhost` / `127.0.0.1` / `https`
- ❌ 일반 `http + IP`(예: 192.168.x.x로 폰 접속) → 차단
- 발표는 **localhost** 에서 하면 배포 없이 웹캠 시연 가능.

### 🌐 오프라인 대비
`html5-qrcode.min.js`(375KB)를 **CDN이 아니라 static 폴더에 저장** → 발표장 인터넷이 끊겨도 스캔 동작.

---

## ✅ 4단계 — 운동 흐름 & 카운트 (workout > machine_use > set_log)

### 🤔 무슨 생각으로
실제 센서가 없으니 **[+1 Rep] 버튼**으로 감지를 시뮬하고,
**마지막 Rep 후 휴식 N초(REST_SEC=8) 무동작이면 세트 자동 확정**한다.
매 Rep마다 서버를 때리면 느리므로, **세트 배열을 [사용 종료] 시 한 번에 POST**한다.

### ⚙️ 동작 방식
```
[운동 시작] → workout 생성(진행중) → /scan/
[QR 스캔]   → /machine/<id>/?wid=...
[이 머신으로 운동] POST /use/start/ → machine_use 생성 → /use/<id>/
[카운트 화면] /use/<id>/
   [+1 Rep] reps++ → 8초 무동작 시 세트 확정(JS) → sets 배열 누적
   ├ [사용 종료]  POST /use/<id>/end/             → set_log 저장 → /scan/ (다음 머신)
   └ [운동 종료]  POST /use/<id>/end/ (then_end=1) → 사용 저장 + 운동 전체 종료
```

### 🔑 핵심 코드 ① — 세트 배열을 JSON으로 한 번에 저장
```python
sets = json.loads(request.form.get('sets_json', '[]'))   # [{"reps":12,"weight":40}, ...]
for s in sets:
    reps = int(s.get('reps', 0) or 0)
    if reps <= 0: continue
    n += 1
    cursor.execute('INSERT INTO set_log (machine_use_id, set_number, reps, weight) VALUES (?,?,?,?)',
                   (use_id, n, reps, w))
recalc_workout(cursor, wid)        # 총 볼륨 재계산
```

### 🔑 핵심 코드 ② — [사용 종료] vs [운동 종료] 분기
```python
if request.form.get('then_end') == '1':       # 카운트 화면에서 바로 운동 종료
    reward = _finalize_workout(cursor, uid, wid)
    return redirect(f'/workout/{wid}/done/')
return redirect('/scan/')                      # 그냥 사용만 종료 → 다음 머신 스캔
```

### 🔑 핵심 코드 ③ — 운동 종료 처리 + 루틴 해금
```python
def _finalize_workout(cursor, uid, wid):
    dur = (now - started).total_seconds()                  # 운동 시간
    vol = recalc_workout(cursor, wid)                      # 총 볼륨
    cursor.execute("UPDATE workout SET status='완료', ended_at=?, duration_sec=?, total_volume=? WHERE id=?", ...)
    if w[3]:   # 루틴이면
        그날 is_completed=1 → 다음 날 is_unlocked=1        # 해금
        reward = check_reward(cursor, uid, routine_id)     # 보상 체크
```

---

## ✅ 5단계 — 루틴 95% 완료 → 보상 쿠폰 🎁

### 🤔 무슨 생각으로
"운동 → 보상" 동기부여 루프의 마무리. 30일 루틴을 거의 다 하면 제휴 쿠폰을 준다.

### 🔑 핵심 코드 — 95% + 미지급일 때만 1회 지급
```python
def check_reward(cursor, uid, routine_id):
    done, total, rate = routine_progress(cursor, routine_id)
    if rate < 95: return None
    if reward_given: return None                       # 중복 지급 방지
    c = random.choice(재고>0 쿠폰들)                    # 무작위 지급
    cursor.execute('INSERT INTO user_coupon (...) VALUES (...)')
    cursor.execute('UPDATE coupons SET total_qty=total_qty-1 WHERE id=?', (c[0],))  # 재고 -1
    cursor.execute("UPDATE routine SET reward_given=1, status='완료' WHERE id=?", ...)
    return ucid
```
> 지급된 `user_coupon.id`를 `session['new_reward']`에 담아 요약 화면에서 **1회성 축하**(`session.pop`)로 표시.

---

## ✅ 6단계 — 커스텀 루틴 / 리포트 / 내 정보

### 커스텀 루틴 (`/custom/`)
종목을 골라 세트×횟수로 담아 저장 → [시작] 하면 `workout(source=커스텀)` 으로 ④ 흐름 진행.

### 리포트 (`/report/`)
- **캘린더**: 이번 달, 운동 완료한 날 표시. 날짜 터치 → 그날 운동 상세(`/report/day/<date>/`).
- **부위 히트맵**: 최근 7일 부위별 볼륨(`set_log` 집계).
- **최근 기록 5개** + **총 통계**(운동 수·시간·볼륨).
- **내 무게 차트**: `weight_log` 최근 30일 → SVG 폴리라인(`build_weight_chart`), [기록] 버튼으로 추가.

### 내 정보 (`/me/`)
가입 정보 조회/수정(`/me/edit/`), 루틴 진행률·보유 쿠폰 수, 보상함(`/me/rewards/`).

---

## ✅ 7단계 — 관리자 (슬림 CRUD) — 웹DB 평가용

### 🤔 무슨 생각으로
평가 기준의 **"CRUD + 관리자 페이지"** 를 정면 충족. 핵심만 슬림하게.

### 🔑 핵심 코드 — 이중 차단
```python
def admin_guard():
    if not require_login(): return redirect('/login/')   # ① 로그인?
    if not require_admin(): return redirect('/')          # ② role==admin?
    return None
```

### ⚙️ 기능
```
/admin/            대시보드: 회원·종목·완료운동·쿠폰 수 + 인기 종목 TOP5(LEFT JOIN)
/admin/exercises/  운동 종목 추가/삭제
/admin/coupons/    쿠폰 추가/삭제 (+ 지급 횟수)
/admin/users/      회원 목록/삭제 (본인 삭제 방지)
```

---

## ✅ 8단계 — 보안 점검 & 취약점 수정 (2026-06-04)

### 🤔 무슨 생각으로
기능 완성 후 "보안적으로 문제가 없는지" 전체 코드를 점검했다.
다행히 **SQL 인젝션·XSS는 안전**(전 쿼리 파라미터 바인딩 `?`, Jinja2 자동 이스케이프, `|safe` 미사용)했으나,
**인증/세션 쪽에 치명적 문제**가 있어 우선순위 높은 것부터 수정했다.

### 🔑 수정 ① — secret_key 하드코딩 제거 (가장 치명적)
**문제**: `app.secret_key = 'fitqr_secret_2026_v2'` 가 코드에 박혀 있었다.
- Flask 세션은 **서버가 아니라 쿠키(클라이언트)에 저장**되고, 위조 방지는 오직 `secret_key`로 만든 HMAC 서명뿐.
- 키가 코드/깃에 노출 + 추측 가능 → 공격자가 `flask-unsign` 등으로 **`role=admin` 세션 쿠키를 위조** → 로그인 없이 관리자 권한 탈취 가능.

**해결**: 키를 코드에서 분리.
```python
def load_secret_key():
    # 우선순위: 환경변수 > 로컬 .secret_key 파일 > 새로 무작위 생성 후 저장
    env = os.environ.get('SECRET_KEY')
    if env: return env
    key_path = os.path.join(BASE_DIR, '.secret_key')
    if os.path.exists(key_path):
        saved = open(key_path).read().strip()
        if saved: return saved
    key = secrets.token_hex(32)          # 64자리 예측 불가능한 무작위 키
    open(key_path, 'w').write(key)
    return key

app.secret_key = load_secret_key()
```
> `.secret_key` 는 `.gitignore` 에 추가 → 키가 저장소에 절대 올라가지 않음.
> 환경변수가 있으면 그걸 쓰고(배포), 없으면 파일에 한 번 생성해 재사용(개발/시연 시 재시작해도 로그인 유지).

### 🔑 수정 ② — 관리자 권한을 DB에서 재확인 (심층 방어)
**문제**: `require_admin()` 이 `session.get('role')` 만 믿었다 → 쿠키 위조 시 곧바로 통과.
**해결**: 세션이 아니라 **DB의 실제 role**을 매번 확인.
```python
def require_admin():
    if 'user_id' not in session: return False
    cursor.execute('SELECT role FROM users WHERE id=?', (session['user_id'],))
    row = cursor.fetchone()
    return bool(row) and row[0] == 'admin'
```
> secret_key가 새더라도 DB role이 user면 관리자 진입 차단(이중 안전장치).

### 🔑 수정 ③ — 비밀번호 평문 저장 → 해시 (werkzeug)
**문제**: `users.password` 가 평문으로 저장되고, 로그인도 `WHERE login_id=? AND password=?` 평문 비교였다.
→ DB 유출 시 모든 회원 비번 그대로 노출(타 서비스 비번 재사용 피해까지).

**해결**: `werkzeug.security` 의 해시 사용.
```python
from werkzeug.security import generate_password_hash, check_password_hash

# 가입/시드: 해시로 저장
generate_password_hash(password)        # 예: 'scrypt:32768:8:1$....'

# 로그인: 아이디로 조회 후 해시 검증
cursor.execute('SELECT * FROM users WHERE login_id=?', (login_id,))
if is_hashed(stored):
    ok = check_password_hash(stored, password)
elif stored == password:                # 레거시 평문이면 일치 시 해시로 자동 승급
    ok = True
    cursor.execute('UPDATE users SET password=? WHERE id=?', (generate_password_hash(password), uid))
```
> **기존 DB 호환**: `init_db()` 에서 평문 비번을 **1회 자동 마이그레이션**(`is_hashed()` 로 판별 → 아니면 해시로 UPDATE).
> 이미 깔린 `fitqr.db` 의 admin/회원 비번도 첫 실행 시 자동으로 해시 전환되어, **로그인 흐름이 깨지지 않는다.**
> `is_hashed(pw)` = `pw.startswith(('pbkdf2:', 'scrypt:'))`.

### 🔑 수정 ④ — CSRF 보호 (의존성 없이 직접 구현)
**문제**: 모든 상태변경 POST(회원정보 수정, 운동/사용 종료, 커스텀·관리자 삭제 등)에 CSRF 토큰이 없어,
공격자 사이트가 자동 제출 폼으로 관리자 몰래 위험 동작을 유발할 수 있었다.

**해결**: `Flask-WTF` 설치 없이 직접 구현(추가 패키지 불필요).
```python
def generate_csrf_token():                      # 세션에 무작위 토큰 1개
    if '_csrf_token' not in session:
        session['_csrf_token'] = secrets.token_hex(16)
    return session['_csrf_token']

@app.context_processor                          # 템플릿에서 {{ csrf_token() }}
def inject_csrf():
    return {'csrf_token': generate_csrf_token}

@app.before_request                             # 모든 POST 검증
def csrf_protect():
    if request.method == 'POST':
        sent = request.form.get('_csrf', '')
        real = session.get('_csrf_token', '')
        if not real or not hmac.compare_digest(str(sent), str(real)):
            return ('CSRF 검증 실패: 새로고침 후 다시 시도하세요.', 400)
```
```html
<!-- 각 POST 폼 HTML에 토큰을 서버사이드로 직접 삽입 (JS 무관, 20개 폼 전부) -->
<input type="hidden" name="_csrf" value="{{ csrf_token() }}">
```
> **왜 서버사이드로?** 처음엔 base.html JS로 모든 폼에 자동 주입했으나, **배포 시 JS 로딩 실패/지연이면 토큰 누락 → POST 400** 위험이 있었다.
> → 토큰을 **각 폼 HTML에 직접 박아** JS 없이 항상 포함되게 변경(가장 안전). base.html JS는 "토큰 없는 폼만" 보완하는 안전망으로 유지(중복 주입 안 함).
> 검증: 파일별 `method="post"` 폼 수 = `_csrf` 필드 수 정확히 일치(누락 0).

### 🔑 수정 ⑤ — 세션 쿠키 보안 플래그
**문제**: 쿠키에 보안 플래그 미설정 → JS로 세션 탈취(XSS) / 타 사이트 요청에 쿠키 전송(CSRF) 가능.
**해결**:
```python
app.config.update(
    SESSION_COOKIE_HTTPONLY=True,    # JS(document.cookie)로 세션 읽기 차단
    SESSION_COOKIE_SAMESITE='Lax',   # 타 사이트發 요청엔 쿠키 미전송(CSRF 완화)
    # SESSION_COOKIE_SECURE=True,    # HTTPS 배포 시 주석 해제(http에선 켜면 로그인 깨짐)
)
```

### 🔑 기본 관리자 계정(admin/1234) — 결정: 시연용 유지
시연 편의(예측 가능한 로그인)를 위해 **admin/1234 그대로 유지**하되, 코드 시드에
`⚠️ 시연용 기본 계정 — 배포 시 반드시 변경` 주석을 달아 명시. (실 배포 시 비번 변경 권장)

### 🔑 수정 ⑥~⑩ — 나머지 취약점 보완 (2026-06-04)
| # | 문제 | 해결 |
|---|------|------|
| ⑥ | 로그인 **brute-force** | 인메모리 IP별 실패 카운트 — 최근 5분 5회 실패 시 잠금(`login_blocked`/`record_login_fail`/`clear_login_fails`). 단일 프로세스 기준(시연·무료 배포 충분) |
| ⑦ | 스캔 **QR 오픈리다이렉트/DOM XSS** | `scan.html`이 `/machine/<숫자>/` 패턴만 추출해 **내부 상대경로로만 이동**. 패턴 없으면 이동 안 함. `new URL()`/`decodedText` 직접 이동 폴백 제거 + `encodeURIComponent` |
| ⑧ | 사용자명 **enumeration** | 로그인 실패 메시지 통일 + **사용자 없을 때도 더미 해시 검증**(`_DUMMY_HASH`)으로 응답 시간 차 제거 |
| ⑨ | **HTTPS 미적용** | 환경변수 `FORCE_HTTPS=1`(배포)에서 http→https 301 + HSTS + 쿠키 Secure. 로컬은 영향 없음 |
| ⑩ | 입력 변환 **500** | `to_float`/`to_int` 안전 변환으로 전 입력 교체(빈도·요일 범위 강제, sets_json 타입 검증) |
| + | 공통 **보안 헤더** | 모든 응답에 `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options: nosniff` |

### ✅ 점검 결과 — 원래부터 안전한 부분 (발표 어필)
- **SQL 인젝션 안전**: 전 쿼리 `?` 파라미터 바인딩(LIKE 포함). 문자열 포맷 SQL 없음.
- **XSS 기본 방어**: Jinja2 자동 이스케이프, `|safe`/`render_template_string`/`eval`/`exec` 미사용.
- **IDOR 방지**: 모든 자원 조회에 `user_id` 일치 확인.
- **관리자 이중 차단 + 본인 삭제 방지**, **유령 세션 정리 + 로그인 시 `session.clear()`**.

### ⚠️ 남은 권장 사항 (발표 시 "향후 개선점")
| 우선 | 항목 | 결정 |
|------|------|------|
| 높음 | `debug=True` 실행 | **결정: 개발 중 유지, 배포 시점에 끄기**(2026-06-04) |
| 중간 | 기본 계정 admin/1234 | **결정: 시연용 1234 유지 + 주석 명시**(2026-06-04) |
| 낮음 | brute-force 다중 워커 | 다중 프로세스 배포 시 Redis 등 공유 카운터로(현재 불필요) |

### ✔️ 검증 (test_client)
- secret_key 64자리 로드 + `.secret_key` 자동 생성/gitignore 확인.
- **비번 해시**: admin `scrypt:...` 마이그레이션, 1234 로그인 302, 오답 차단, 신규 가입 해시 저장.
- **CSRF**: 토큰 없음/오답 → 400, 정상 → 302. 폼 HTML에 토큰 박힘(JS 무관).
- **쿠키**: `HttpOnly`·`SameSite=Lax` 포함. 보안헤더 `X-Frame-Options`·`nosniff` 포함.
- **brute-force**: 5회 실패 후 6번째(정답이어도) 잠금 확인.
- **enumeration**: 없는 아이디/틀린 비번 메시지 동일 확인.
- **입력 안전**: 숫자 칸에 문자 입력해도 500 아님(302 정상 처리).
- **HTTPS off 기본**: 로컬 http 접속 정상(리다이렉트 안 걸림).

---

## ✅ 9단계 — 운동 시작 방식 개편(QR/수동) + 발표용 QR (2026-06-04)

### 🤔 무슨 생각으로
"계획된 종목을 누르면 **QR 스캔 없이 바로** 운동이 시작"되던 문제를 고쳤다.
머신은 QR로 시작해야 운동 내역이 정확히 남고, **QR이 훼손되면 수동으로도** 시작할 수 있어야 한다.
또 발표 때는 **세트/무게가 미리 박힌 QR**을 스캔해 한 번에 시연하고 싶다.

### ⚙️ 운동 시작 2-경로 (선택 화면)
계획된 종목 [시작] → `/use/choose/<eid>/` (선택 화면)
- 머신 종목: **[📷 QR 스캔하기]** + **[✍️ 수동으로 시작하기]**
- 비머신 종목: **[✍️ 수동으로 시작하기]** (QR 없음)

### ⚙️ 카운트 3-모드 (`use.html`)
| 모드 | 진입 | 동작 |
|------|------|------|
| **auto** | QR 스캔 후 | `+1 Rep` 자동 카운트 + 휴식 8초 무동작 시 세트 자동완료 (기존 그대로) |
| **manual** | [수동으로 시작하기] | 무게·횟수 직접 입력, **세트 종료 직접**(자동 휴식 없음) |
| **demo** | 발표용 QR 스캔 | `sets` 배열이 **자동으로 미리 채워짐** → [사용 종료]/[운동 종료]만 누름 |

```python
# use_start: 폼 값으로 진입 모드 결정
mode = request.form.get('mode','auto')
psets, preps, pweight = to_int(...sets), to_int(...reps), to_float(...weight)
if psets and preps:   q = f'?demo=1&sets={psets}&reps={preps}&weight={pweight}'
elif mode=='manual':  q = '?mode=manual'
else:                 q = ''
return redirect(f'/use/{use_id}/{q}')
```

### 🔑 발표용 QR (`make_demo_qr.py`)
부위별 대표 머신 + 현실 세트/무게를 QR에 박아 PNG로 저장 → `static/demoqr/`.
- 스캔 시 `scan.html`이 **`/machine/<숫자>/` + 숫자 파라미터(sets/reps/weight)만** 추출(보안 유지) → use 화면 demo 모드 자동 채움.
- 부위별: 가슴4×12×40 · 등4×12×45 · 어깨4×12×25 · 팔4×15×25 · 복근3×15×30 · 엉덩이4×12×60 · 다리4×12×120 · 전신3×15×30.
- 실행 `python make_demo_qr.py` → 8개 PNG + 미리보기 `index.html`.

### ✔️ 검증
- 선택 화면(QR/수동) 노출, 수동 모드 UI(세트 종료 버튼), 발표용 머신 페이지 안내·demo 자동입력 확인(test_client).
- 발표용 QR PNG 8개(부위별) 생성 확인.

### 🐞 함께 고친 것 — "스캔 안 했는데 QR 스캔됨"
`/machine/<id>/`가 진입 경로와 무관하게 "QR 인식 완료" 배너를 띄우던 문제 →
**실제 스캔(`?scanned=1`)일 때만** 배너 표시, 클릭 진입은 "머신 정보"로.

---

## 🎉 전체 루프 완성 + 보안 강화 완료
**회원가입 → 30일 루틴 → QR 스캔(자동)/수동 → 운동/카운트 → 기록(리포트) → 보상 + 관리자(CRUD)** 가 모두 동작.
+ **보안 점검 11건**(SQL/XSS 원래 안전 + secret_key·권한·비번해시·CSRF·쿠키·brute-force·QR·enumeration·HTTPS·입력검증 보완) 완료.
+ **운동 시작 QR/수동 분기 + 발표용 QR** 추가.

---

## 🔐 기본 계정 정보
| 구분 | 아이디 | 비번 |
|------|--------|------|
| 관리자 | admin | 1234 |
| 일반 회원 | 직접 가입 (가입 시 30일 루틴 자동 생성 + 자동 로그인) | |

## ▶️ 실행
```bash
python app.py     # http://localhost:5000
```
> `init_db()` 가 모듈 로드 시 자동 실행되어 11테이블 + 시드를 준비한다.
