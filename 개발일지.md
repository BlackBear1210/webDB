# 🏋️ FitQR 개발일지

> 각 단계에서 **무슨 생각으로 / 어떻게 / 왜** 그렇게 구현했는지 기록하는 문서.
> 발표 준비 & README 작성 시 이 문서를 그대로 활용하면 됩니다.

---

## 📐 전체 설계 원칙

1. **교수님 PDF(11회차) 패턴을 그대로 따른다.**
   - `get_db()` 로 DB 연결, `init_db()` 로 테이블 생성
   - `session` 으로 로그인 상태 관리, `role` 로 관리자 구분
   - `render_template()` + Jinja2(`{{ }}`, `{% %}`) 로 화면 분리
   - → 채점자(교수님)에게 익숙한 구조 = 안정적이고 검증된 방식

2. **"세로로 쌓기" 전략**
   - 한 기능을 *DB → 백엔드 → 화면* 까지 끝까지 완성하고 다음으로 넘어감
   - 항상 "돌아가는 상태"를 유지 → 언제 발표해도 시연 가능

3. **오류 방지를 최우선으로**
   - 각 단계마다 Flask `test_client` 로 자동 테스트 + 실제 서버 실행 확인
   - 숫자 입력값은 빈 값이 들어와도 안 터지게 안전 변환

---

## ✅ 0단계 — 프로젝트 토대 + DB 설계

### 🤔 무슨 생각으로
모든 기능이 DB 위에서 돌아가므로, **테이블 구조를 먼저 확정**해야 뒤가 꼬이지 않는다.
12개 테이블을 한 번에 만들고, 발표에서 바로 보여줄 수 있도록 **시드(seed) 데이터**(기구·운동·쿠폰)를 미리 채워 넣었다.

### 📁 폴더 구조
```
fitqr/
  ├── app.py            # 메인 (라우트 + DB)
  ├── fitqr.db          # SQLite DB (자동 생성)
  ├── templates/        # HTML (화면)
  └── static/           # CSS, 이미지
```

### 🗄️ DB 테이블 12개
| # | 테이블 | 역할 |
|---|--------|------|
| 1 | users | 회원 (아이디/비번/이름/신체정보/role) |
| 2 | health_profile | 건강상태 (3대무게/지병/수술/수면/목표) |
| 3 | machines | 운동 기구 (이름/부위/최소가동범위/QR) |
| 4 | workout_session | 운동 세션 (기구별 운동 시작~종료) |
| 5 | set_log | 세트별 기록 (무게/횟수/유효여부) |
| 6 | exercise_master | 추천용 운동 DB (일반/재활) |
| 7 | routine | 추천 루틴 헤더 (목표/완료율) |
| 8 | routine_item | 루틴 구성 운동 |
| 9 | diet_plan | 추천 식단 헤더 (칼로리/매크로) |
| 10 | diet_item | 식단 구성 메뉴 |
| 11 | coupons | 할인쿠폰 마스터 |
| 12 | user_coupon | 회원이 받은 쿠폰 |

### 🔑 핵심 코드 — DB 연결 (PDF 패턴 그대로)
```python
def get_db():
    conn = sqlite3.connect('fitqr.db')
    cursor = conn.cursor()
    return conn, cursor
```

### 🔑 핵심 코드 — 시드 데이터 (중복 방지)
```python
# 이미 데이터가 있으면 또 넣지 않도록 COUNT 로 체크
cursor.execute('SELECT COUNT(*) FROM machines')
if cursor.fetchone()[0] == 0:
    machines_seed = [
        ('랫풀다운', '등', 15, '광배근...'),
        ('체스트프레스', '가슴', 10, '대흉근...'),
        # ... 8개
    ]
    for m in machines_seed:
        cursor.execute('INSERT INTO machines (...) VALUES (?,?,?,?)', m)
```
> **min_valid_rom**(최소 가동범위) 컬럼이 핵심 아이디어 ―
> 이 값보다 적게 당기면 "카운트 안 됨" 처리에 사용된다.

### 시드 데이터 현황
- 기구 8개 / 운동종목 25개(일반 15 + 재활 10) / 쿠폰 5개 / 관리자 계정 1개

### ✔️ 검증
- 13개 테이블(12개 + sqlite_sequence) 생성 확인, 시드 데이터 건수 확인 완료

---

## ✅ 1단계 — 회원가입 / 로그인 / 로그아웃

### 🤔 무슨 생각으로
앞으로 만들 **모든 기능이 "로그인한 회원" 기준**으로 동작한다.
(누가 운동했는지, 누구의 루틴인지, 누구에게 쿠폰을 줄지 모두 `user_id` 가 필요)
그래서 인증을 가장 먼저 만들어 토대를 세웠다.

### ⚙️ 동작 방식 (흐름도)
```
[회원가입]  /register/
   GET  → 빈 폼 보여줌
   POST → 아이디 중복확인 → 없으면 users 테이블에 INSERT → 성공 메시지

[로그인]   /login/
   GET  → 빈 폼
   POST → 아이디+비번 일치 확인
        → 맞으면 session 에 user_id/login_id/name/role 저장 → 메인('/')
        → 틀리면 에러 메시지

[로그아웃] /logout/
   session.clear() → 로그인 페이지로

[접근 차단] 메인('/')
   session 에 user_id 없으면 → /login/ 으로 강제 이동
```

### 🔑 핵심 코드 — 로그인 (session 에 정보 저장)
```python
@app.route('/login/', methods=['GET', 'POST'])
def login():
    if request.method == 'POST':
        login_id = request.form['login_id']
        password = request.form['password']

        conn, cursor = get_db()
        cursor.execute('SELECT * FROM users WHERE login_id = ? AND password = ?',
                       (login_id, password))
        user = cursor.fetchone()
        conn.close()

        if user:
            session['user_id']  = user[0]   # 이후 모든 기능에서 사용
            session['login_id'] = user[1]
            session['name']     = user[3]
            session['role']     = user[8]   # 'user' 또는 'admin'
            return redirect('/')
        else:
            return render_template('login.html', error='아이디 또는 비밀번호가 틀렸습니다.')
    return render_template('login.html', error='')
```
> **왜 `?` 를 쓰나?** SQL 인젝션 방지 + 따옴표 오류 방지 (PDF에서 강조한 방식).

### 🔑 핵심 코드 — 비로그인 차단 (PDF 패턴)
```python
@app.route('/')
def index():
    if 'user_id' not in session:      # 로그인 안 했으면
        return redirect('/login/')    # 로그인 페이지로 쫓아냄
    return render_template('index.html')
```

### 🔑 핵심 코드 — 아이디 중복 방지
```python
cursor.execute('SELECT * FROM users WHERE login_id = ?', (login_id,))
if cursor.fetchone():                 # 이미 있으면
    return render_template('register.html', error='이미 존재하는 아이디입니다.', success='')
```

### 🔑 핵심 코드 — 숫자 입력 안전 변환 (오류 방지)
```python
# 나이/키/몸무게를 비워도 서버가 안 터지게 처리
age    = int(request.form['age'])     if request.form.get('age')    else 0
height = float(request.form['height']) if request.form.get('height') else 0
weight = float(request.form['weight']) if request.form.get('weight') else 0
```

### 🎨 화면 (templates)
| 파일 | 내용 |
|------|------|
| login.html | 로그인 폼 + 회원가입 링크 |
| register.html | 회원가입 폼 (성공 시 폼 숨기고 로그인 버튼 표시) |
| index.html | 대시보드 (이름·관리자배지·로그아웃 + 기능 메뉴) |
| static/style.css | 전 페이지 공통 디자인 (붉은색 피트니스 테마) |

> Jinja2 조건문으로 관리자에게만 배지/메뉴를 보여준다:
> ```html
> {% if session['role'] == 'admin' %}
>     <span class="role-badge">관리자</span>
> {% endif %}
> ```

### ⚠️ 발견한 버그 & 해결
- **증상**: 실제 `python app.py` 실행 시 `UnicodeEncodeError` 로 서버가 죽음.
- **원인**: `print("✅ ...")` 의 이모지를 Windows 콘솔(cp949)이 출력 못 함.
- **해결**: print 문을 영문(`[FitQR] DB init done`)으로 변경.
- **교훈**: Windows 콘솔에 이모지 print 금지.

### ✔️ 검증 (10개 자동 테스트 전부 통과)
| 테스트 | 결과 |
|--------|------|
| 비로그인 메인 접근 → 로그인 리다이렉트 | ✅ 302 |
| 로그인/회원가입 페이지 GET | ✅ 200 |
| 회원가입 성공 | ✅ |
| 중복 아이디 차단 | ✅ |
| 틀린 비밀번호 차단 | ✅ |
| 정상 로그인 → 세션 유지 | ✅ |
| 관리자 로그인 + 배지 표시 | ✅ |
| 로그아웃 후 재차단 | ✅ |
| 실제 서버 실행 (login/static/register) | ✅ 200 |

---

## ✅ 2단계 — QR 운동 흐름 ⭐ (이 프로젝트의 심장)

### 🤔 무슨 생각으로
이 프로젝트의 **핵심 아이디어**(지쿠처럼 QR 찍고 운동하면 자동 카운트)를 구현하는 단계.
실제 센서/하드웨어는 못 만드니까 **웹으로 시뮬레이션**한다:
- "QR 스캔" = 기구별 URL(`/machine/<id>`) → 나중에 QR 이미지로 변환
- "센서 자동 카운트" = 화면의 슬라이더(가동범위) + 당기기 버튼
- "일정 범위 미달이면 카운트 안 됨" = 슬라이더 값 < 기구의 `min_valid_rom` 이면 무효
- "종목 종료" = DB(set_log)에 무게·세트·횟수 저장

### ⚙️ 동작 방식 (흐름도)
```
기구목록(/machines/)
   └─ 기구클릭 또는 QR스캔 → /machine/<id> (무게 설정)
        └─ [운동 시작] → workout_session 생성(status='진행중')
             └─ /workout/<session_id> (Rep 카운트 화면)
                  ├─ 슬라이더로 가동범위 조절 → [당기기]
                  │     · rom >= MIN_ROM → 카운트 ✅
                  │     · rom <  MIN_ROM → 무효 ❌ (카운트 안 됨)
                  ├─ [세트 완료] → set_log 에 INSERT, 다음 세트로
                  └─ [종목 종료] → status='완료', ended_at 기록
                       └─ /workout/<id>/done (요약: 총 세트·총 횟수)
                            └─ [다음 기구 운동] 으로 이어서
```

### 🔑 핵심 코드 ① — 운동 시작 (세션 생성 + 방금 만든 번호 받기)
```python
cursor.execute(
    '''INSERT INTO workout_session (user_id, machine_id, weight, target_sets, started_at, status)
       VALUES (?, ?, ?, ?, ?, '진행중')''',
    (session['user_id'], id, weight, target_sets, now)
)
session_id = cursor.lastrowid    # 방금 INSERT 한 행의 id → 바로 그 운동 화면으로 이동
return redirect(f'/workout/{session_id}/')
```

### 🔑 핵심 코드 ② — "센서 자동 카운트" 시뮬레이션 (workout.html JS)
```javascript
const MIN_ROM = {{ ws[10] }};   // DB의 기구별 min_valid_rom 값을 그대로 주입
let reps = 0;
function doRep(){
    const rom = parseInt(document.getElementById('rom').value);  // 당긴 범위
    if(rom >= MIN_ROM){          // 가동범위 충족
        reps++;                  // → 카운트
        document.getElementById('reps').value = reps;  // 폼에 저장
    } else {                     // 가동범위 미달
        // → 카운트 안 됨 (❌ 메시지만 표시)
    }
}
```
> **핵심**: 기구마다 `min_valid_rom` 이 다르다(예: 랫풀다운 15cm, 레그프레스 20cm).
> 그 값을 DB에서 가져와 JS에 주입 → "이 기구는 이만큼 당겨야 인정" 로직이 데이터 기반으로 동작.

### 🔑 핵심 코드 ③ — 기구 정보를 JOIN 으로 한 번에 가져오기
```python
cursor.execute('''SELECT ws.*, m.name, m.body_part, m.min_valid_rom
                  FROM workout_session ws
                  JOIN machines m ON ws.machine_id = m.id
                  WHERE ws.id = ?''', (session_id,))
```
> 세션과 기구 두 테이블을 JOIN → 운동 화면에서 기구 이름·부위·가동범위를 한 번에 사용.

### 🔑 핵심 코드 ④ — 보안: 남의 세션 / 없는 세션 차단
```python
ws = cursor.fetchone()
if not ws or ws[1] != session['user_id']:   # 없거나(남의 것) → 차단
    return redirect('/machines/')
if ws[7] == '완료':                          # 이미 끝난 운동 → 요약으로
    return redirect(f'/workout/{session_id}/done/')
```
> URL로 `/workout/999/` 같은 걸 직접 쳐도 안 터지게 방어.

### 🎨 화면 (templates)
| 파일 | 내용 |
|------|------|
| machines.html | 기구 8개 그리드 (클릭 = QR 스캔 진입) |
| machine.html | 기구 상세 + 무게/세트 설정 (QR 진입 화면) |
| workout.html | Rep 카운터 (슬라이더+당기기), 세트완료/종목종료 |
| workout_done.html | 운동 요약 (총 세트·총 횟수, 세트별 기록) |

### ⚖️ 설계 선택 — Rep 카운트를 JS로 한 이유
- **세트 진행 중 매 Rep마다 서버 요청 → 느리고 DB 부담**
- 그래서 **Rep 카운트는 브라우저(JS)에서 실시간 처리**,
  **세트가 끝났을 때만** 그 세트 결과를 DB(set_log)에 저장 → 빠르고 깔끔.

### ✔️ 검증 (10개 자동 테스트 + DB 확인 통과)
| 테스트 | 결과 |
|--------|------|
| 기구 목록 8개 표시 | ✅ |
| 기구 상세(QR 진입) | ✅ |
| 없는 기구 번호 차단 | ✅ 302 |
| 운동 시작 → 세션 생성 | ✅ |
| 세트 3개 저장 → 4세트째 표시 | ✅ |
| 종목 종료 → 완료 처리 | ✅ |
| 완료 요약(총 횟수 합산) | ✅ |
| 완료 세션 재진입 차단 | ✅ |
| 남의 세션 접근 차단 | ✅ |
| 실제 서버 실행 (에러 없음) | ✅ |

**DB 저장 확인**:
```
세션: (1, machine_id=1, 50.0kg, '완료', ended_at 기록됨)
세트: [(1세트,12회,50kg,유효), (2세트,10회,50kg,유효), (3세트,8회,50kg,유효)]
```

---

## 🎨 디자인 리뉴얼 — 갤럭시 S25 폰 앱 UI

### 🤔 무슨 생각으로
발표를 노트북으로 하지만 **"우리가 만든 헬스 폰 앱"** 처럼 보이게 하려고
화면 전체를 **갤럭시 S25 폰 목업** 안에 넣었다. (화면구성 점수 ↑)
QR 시연은 핸드폰 대신 **노트북 웹캠**으로 대체할 예정.

### ⚙️ 동작 방식
- **공통 레이아웃** `base.html` 도입 (Jinja2 `{% extends %}` 상속)
  → 모든 페이지가 폰 프레임 + 상태바 + 하단 탭바를 공유 (중복 제거)
- 갤럭시 S25 비율 반영: **375 × 812px(19.5:9), 펀치홀 카메라, One UI 상태바**
- 하단 **탭바**(홈/운동/기록/추천/내정보) → 진짜 앱 느낌
- 실시간 시계(JS)로 상태바 시간 표시
- **반응형**: 실제 폰(배포 후)에서 접속하면 프레임이 사라지고 풀스크린

### 🔑 핵심 코드 — 폰 프레임 + 반응형
```css
.phone-frame {            /* 데스크탑: 폰 목업 */
    width: 375px; height: 812px;
    border-radius: 46px; padding: 12px;
    background: #0a0a0a;          /* 베젤 */
}
@media (max-width: 440px) {       /* 진짜 폰: 프레임 제거 */
    .phone-frame { width:100%; height:100vh; border-radius:0; padding:0; }
    .punch-hole { display:none; }
}
```

### 🔑 핵심 코드 — 탭바 (로그인 시에만 + 현재 탭 강조)
```html
{% if session['user_id'] %}
<nav class="tabbar">
  <a href="/" class="tab {% if request.path == '/' %}active{% endif %}">🏠 홈</a>
  <a href="/machines/" class="tab {% if '/machine' in request.path %}active{% endif %}">💪 운동</a>
  ...
</nav>
{% endif %}
```
> `request.path` 로 현재 위치를 비교 → 라우트 수정 없이 현재 탭 자동 강조.
> 아직 안 만든 탭(기록/추천/내정보)은 `alert('곧 추가됩니다')` 로 처리해 **404 방지**.

### ✔️ 검증
- 7개 페이지 전부 폰 프레임 안에서 정상 렌더 (회귀 테스트 통과)
- 비로그인 시 탭바 숨김 / 로그인 시 표시 확인
- 실제 서버 에러 없음

---

## ✅ 3단계 — 운동 기록 조회 + QR 생성 + 웹캠 스캔

### 🤔 무슨 생각으로
2단계에서 운동을 DB에 저장했으니, 이제 그걸 **조회**하는 화면이 필요하다.
그리고 이 프로젝트의 핵심인 **QR**을 실제로 구현한다:
- QR 이미지를 **동적 생성**(기구별)
- **노트북 웹캠**으로 그 QR을 읽어 운동 화면으로 이동
  (발표 때 핸드폰 대신 노트북 카메라로 시연)

### ⚙️ 동작 방식
```
[운동 기록]  /history/
   내 운동 세션 목록 + 요약통계(총 운동 수, 총 반복 수)
   각 기록 클릭 → 그 운동의 상세(done 화면)

[QR 생성]   /machine/<id>/qr.png
   기구 URL을 QR 이미지(PNG)로 즉석 생성해서 응답

[QR 모아보기] /qrcodes/
   8개 기구 QR을 한 화면에 → 출력하거나 띄워놓고 스캔

[웹캠 스캔]  /scan/
   노트북 카메라 ON → QR 비추면 → 기구번호 추출 → 운동화면 이동
```

### 🔑 핵심 코드 ① — QR 이미지 동적 생성 (도메인 자동)
```python
@app.route('/machine/<int:id>/qr.png')
def machine_qr(id):
    url = url_for('machine', id=id, _external=True)  # 호스트 자동 감지
    img = qrcode.make(url)              # qrcode 라이브러리로 생성
    buf = io.BytesIO()                  # 파일 저장 없이 메모리에서
    img.save(buf, 'PNG'); buf.seek(0)
    return send_file(buf, mimetype='image/png')
```
> **왜 동적 생성?** `url_for(_external=True)` 가 현재 접속 주소를 자동 감지 →
> - 로컬: `http://127.0.0.1:5000/machine/1/`
> - 배포: `https://mygym.pythonanywhere.com/machine/1/`
> **도메인을 코드에 박지 않아도** 환경에 맞는 QR이 나온다. (검증 완료)

### 🔑 핵심 코드 ② — 웹캠 스캔 (html5-qrcode, scan.html)
```javascript
function onScanSuccess(decodedText) {
    const m = decodedText.match(/machine\/(\d+)/);  // URL에서 기구번호 추출
    if (m) {
        scanner.stop();
        window.location.href = '/machine/' + m[1] + '/';  // 현재 주소 기준 이동
    }
}
Html5Qrcode.getCameras().then(cameras => {           // 노트북 카메라 목록
    scanner = new Html5Qrcode("reader");
    scanner.start(cameras[0].id, {fps:10, qrbox:220}, onScanSuccess, ()=>{});
});
```
> **포인트**: QR에 전체 URL이 들어있어도 **기구번호만 뽑아** 현재 origin 기준으로 이동.
> → QR 하나로 로컬에서도, 배포 후에도 동일하게 동작.

### 🔑 핵심 코드 ③ — 운동 기록 집계 (서브쿼리)
```python
cursor.execute('''
    SELECT ws.id, m.name, m.body_part, ws.weight, ws.started_at, ws.status,
           (SELECT COUNT(*) FROM set_log WHERE session_id = ws.id),         -- 세트 수
           (SELECT COALESCE(SUM(reps),0) FROM set_log WHERE session_id = ws.id) -- 총 반복
    FROM workout_session ws JOIN machines m ON ws.machine_id = m.id
    WHERE ws.user_id = ? ORDER BY ws.id DESC
''', (session['user_id'],))
```
> 서브쿼리로 세션마다 세트수·총횟수를 한 번에 집계 → "DB 잘 다룬다" 어필.

### 🌐 오프라인 대비
- 웹캠 스캔 라이브러리(`html5-qrcode.min.js`, 375KB)를 **CDN이 아니라 static 폴더에 저장**.
  → 발표장 인터넷이 끊겨도 스캔 동작.
- 카메라 권한은 `localhost`(보안 컨텍스트)에서 정상 허용됨 → **배포 없이도 시연 가능**.

### ⚠️ 주의: 카메라 동작 조건
- 브라우저 카메라는 `localhost` 또는 `https` 에서만 허용됨.
- 로컬 개발(`127.0.0.1`)은 OK, 배포(https)도 OK. (일반 http+IP는 불가)

### ✔️ 검증 (7개 자동 테스트 + 디코딩 확인 통과)
| 테스트 | 결과 |
|--------|------|
| 운동기록 페이지 (2회/총75회 집계) | ✅ |
| QR 이미지 생성 (PNG 시그니처 확인) | ✅ |
| QR 모아보기 페이지 | ✅ |
| 웹캠 스캔 페이지 (라이브러리 로드) | ✅ |
| 스캔 JS 파일 서빙 (375KB) | ✅ |
| 기록 탭 연결 | ✅ |
| 빈 기록 안내 처리 | ✅ |
| QR URL 호스트별 자동 생성 | ✅ (로컬/배포 둘 다 확인) |

---

## ✅ 4단계 — 건강 진단 + 추천 루틴/식단 (규칙 기반 엔진)

### 🤔 무슨 생각으로
이 프로젝트에서 **가장 똑똑해 보이는** 기능. 사용자의 몸 상태·목표를 입력받아
**운동 루틴 + 식단**을 자동 추천한다. AI 없이 **공식 + 조건 분기(규칙 기반)** 으로
구현해서 안정적이고, 발표에서 로직을 설명하기 좋다.

### ⚙️ 동작 방식
```
[건강 진단]  /profile/
   목표(살빼기/근성장/벌크업) + 3대무게 + 지병 + 수술이력 + 수면 + 활동량 입력
   → health_profile 에 upsert (있으면 UPDATE, 없으면 INSERT)

[추천 생성]  /recommend/generate/
   ┌ 지병/수술 있음 → 재활 루틴 (target_disease 매칭)
   └ 건강함        → 목표별 일반 루틴
   + BMR/TDEE 계산 → 목표별 칼로리 식단 생성
   → routine + routine_item + diet_plan + diet_item 에 저장

[추천 보기]  /recommend/
   최신 루틴 + 식단을 표로 표시 (없으면 '추천 받기' 버튼)
```

### 🔑 핵심 코드 ① — 칼로리/매크로 계산 (Mifflin-St Jeor 공식)
```python
def calc_diet(user, profile):
    # 기초대사량(BMR)
    bmr = 10*weight + 6.25*height - 5*age + (5 if gender=='남' else -161)
    # 활동량 계수 → 일일소비칼로리(TDEE)
    factor = {'좌식':1.2, '보통':1.55, '활발':1.725}[activity]
    tdee = bmr * factor
    # 목표별 가감
    adjust = {'살빼기':-400, '근성장':0, '벌크업':+400}[goal]
    kcal = round(tdee + adjust)
    # 매크로: 단백질(체중기반)·지방(25%)·탄수(나머지)
    protein = round(weight * (2.0 if goal=='살빼기' else 1.6))
    fat     = round(kcal * 0.25 / 9)
    carb    = round((kcal - protein*4 - fat*9) / 4)
```
> **검산 통과**: 남/80kg/180cm/25세/활발/벌크업 → 정확히 3514kcal 계산됨.

### 🔑 핵심 코드 ② — 재활 vs 일반 분기
```python
condition = (지병 + ' ' + 수술이력).strip()
if condition:                        # 건강 이슈 있음 → 재활
    exercises = pick_rehab_exercises(cursor, condition)
else:                                # 건강함 → 목표별 일반
    cursor.execute("SELECT * FROM exercise_master WHERE type='일반' AND goal=?", (goal,))
    exercises = cursor.fetchall()[:5]
```

### 🔑 핵심 코드 ③ — 재활 운동 매칭
```python
def pick_rehab_exercises(cursor, condition):
    cursor.execute("SELECT * FROM exercise_master WHERE type='재활'")
    picked = []
    for e in cursor.fetchall():
        target = e[4]   # target_disease
        if (('허리' in condition and '허리' in target) or
            ('무릎' in condition and '무릎' in target) or
            ('어깨' in condition and '어깨' in target)):
            picked.append(e)
    return picked[:5]
```
> **검증**: "허리디스크" 입력 → 맥켄지운동·버드독·데드버그·고양이소운동 (전부 재활) 추천됨.

### 🔑 핵심 코드 ④ — 재추천 시 기존 추천 정리
```python
# 활성 추천 1개만 유지 → 기존 루틴/식단 + 하위 항목 모두 삭제 후 재생성
cursor.execute('SELECT id FROM routine WHERE user_id=?', (uid,))
for (rid,) in cursor.fetchall():
    cursor.execute('DELETE FROM routine_item WHERE routine_id=?', (rid,))
cursor.execute('DELETE FROM routine WHERE user_id=?', (uid,))
```
> 부모(routine) 지우기 전에 자식(routine_item)부터 정리 → 고아 데이터 방지.

### 🎨 화면 (templates)
| 파일 | 내용 |
|------|------|
| profile.html | 건강 진단 입력 폼 (기존값 자동 채움) |
| recommend.html | 추천 루틴 표 + 식단(칼로리/매크로/끼니) 표 |

### ✔️ 검증 (6개 케이스 통과)
| 테스트 | 결과 |
|--------|------|
| 진단 전 추천 접근 → 진단으로 유도 | ✅ |
| 진단 저장(upsert) → 추천 이동 | ✅ |
| 벌크업 → 일반 루틴 5개 + 식단 3514kcal | ✅ |
| 허리디스크 → 재활 루틴(전부 재활운동) | ✅ |
| 재추천 시 루틴 1개만 유지 | ✅ |
| 대시보드/탭 연결 | ✅ |
| 실제 서버 에러 없음 | ✅ |

---

## ✅ 5단계 — 루틴 완료 → 보상 쿠폰 지급 🎁

### 🤔 무슨 생각으로
추천 루틴을 **완벽하게 수행하면 제휴 할인코드를 보상**으로 주는 기능.
"운동 → 보상" 의 동기부여 루프를 완성해 **발표 임팩트의 화룡점정**으로 삼는다.
(운동 3~5개 완료 → 쿠폰 팡! → 보상함에 등장 = 시연 클라이맥스)

### ⚙️ 동작 방식
```
추천 루틴의 각 운동 옆 [완료] 버튼
   └─ 누를 때마다 is_done=1, completion_rate 재계산 (진행률 바 차오름)
        └─ 100% 도달?
             ├─ YES & 아직 보상 안 줌 → 쿠폰 1개 지급 + 보상함으로 이동(축하)
             └─ NO → 추천 화면 유지
[내 보상함]  /my-rewards/
   받은 쿠폰 목록 (제휴사/할인율/코드/날짜)
   방금 받은 쿠폰은 축하 배너 + 강조 (1회성)
```

### 🔑 핵심 코드 ① — 완료율 재계산 + 보상 트리거
```python
# 완료한 운동 / 전체 운동 → 완료율
cursor.execute('SELECT COUNT(*) FROM routine_item WHERE routine_id=?', (routine_id,))
total = cursor.fetchone()[0]
cursor.execute('SELECT COUNT(*) FROM routine_item WHERE routine_id=? AND is_done=1', (routine_id,))
done = cursor.fetchone()[0]
rate = round(done / total * 100)
cursor.execute('UPDATE routine SET completion_rate=? WHERE id=?', (rate, routine_id))

# 100% + 아직 보상 안 줬으면 → 쿠폰 지급
if rate == 100 and routine_status != '완료':
    cursor.execute("UPDATE routine SET status='완료' WHERE id=?", (routine_id,))
    ...쿠폰 지급...
```
> **중복 보상 방지**: `routine.status != '완료'` 조건으로 100% 도달 "전환 시점"에만 1회 지급.
> (검증: 완료된 항목 다시 눌러도 쿠폰 안 늘어남)

### 🔑 핵심 코드 ② — 쿠폰 지급 + 재고 차감
```python
cursor.execute('SELECT * FROM coupons WHERE total_qty > 0')   # 재고 있는 쿠폰
available = cursor.fetchall()
if available:
    c = random.choice(available)                              # 무작위 지급
    cursor.execute('INSERT INTO user_coupon (user_id, coupon_id, issued_at, is_used) VALUES (?,?,?,0)',
                   (uid, c[0], now))
    session['new_reward'] = cursor.lastrowid                  # 축하용 (1회성)
    cursor.execute('UPDATE coupons SET total_qty = total_qty - 1 WHERE id=?', (c[0],))  # 재고 -1
```
> 마스터(coupons) 재고를 줄이고, 지급내역(user_coupon)에 기록 → **N:M 중간테이블** 활용.

### 🔑 핵심 코드 ③ — 1회성 축하 배너 (session.pop)
```python
new_reward = session.pop('new_reward', None)   # 꺼내면서 동시에 삭제
return render_template('my_rewards.html', coupons=coupons, new_reward=new_reward)
```
> `pop` 으로 한 번 보여주고 사라짐 → 새로고침하면 축하 배너 안 뜸. (검증 완료)

### 🎨 화면 (templates)
| 파일 | 내용 |
|------|------|
| recommend.html (수정) | 진행률 바 + 운동별 [완료] 버튼 + 완료 시 축하 배너 |
| my_rewards.html | 받은 쿠폰 목록 + 신규 쿠폰 강조/축하 |

### ✔️ 검증 (9개 케이스 통과)
| 테스트 | 결과 |
|--------|------|
| 초기 빈 보상함 안내 | ✅ |
| 4/5 완료(80%) → 보상 없음 | ✅ |
| 마지막 완료 → 보상함 리다이렉트 | ✅ |
| 100% 달성 + 쿠폰 지급(나이키 15%) | ✅ |
| 축하 배너 표시 | ✅ |
| 재방문 시 배너 사라짐(1회성) | ✅ |
| 쿠폰 재고 차감(50→49) | ✅ |
| 중복 보상 방지 | ✅ |
| 실제 서버 에러 없음 | ✅ |

---

## 🎉 핵심 기능(1~5단계) 완성!
이제 **회원 → 운동(QR) → 기록 → 추천 → 보상** 의 전체 루프가 동작한다.

## ✅ 관리자 페이지 — 통계 + 회원/기구/쿠폰 관리 (CRUD)

### 🤔 무슨 생각으로
평가 기준에 **"CRUD 및 관리자 페이지 정상 작동(기능완성도 40점)"** 이 명시돼 있어
이걸 정면으로 충족하는 단계. 교수님 PDF의 관리자 패턴(role 체크 → 회원 삭제/권한변경)을
그대로 따르면서, **기구·쿠폰 CRUD**를 추가해 CRUD를 확실히 보여준다.

### 🎨 설계 선택 — 사용자 앱 vs 관리자 콘솔 분리
- **사용자** = 갤럭시 폰 앱(좁은 화면) / **관리자** = PC 관리 콘솔(넓은 데스크탑)
- 관리자는 별도 레이아웃(`admin/admin_base.html`)을 써서 폰 프레임 없이 넓게 표시.
- "교수자 PC에서 관리" 라는 현실 시나리오와도 맞아떨어짐.

### ⚙️ 동작 방식
```
[접근 차단] 모든 /admin/* 라우트 맨 앞에서 이중 확인
   ① 로그인 했나?  ② role == 'admin' 인가?  → 아니면 쫓아냄

[통계]     /admin/            회원수·기구수·세션수·루틴수·쿠폰수 + 인기기구 TOP5
[회원관리] /admin/users/      목록 / 권한변경(user↔admin) / 삭제
[기구관리] /admin/machines/   등록(C)·조회(R)·수정(U)·삭제(D) ← CRUD 전체
[쿠폰관리] /admin/coupons/    등록(C)·조회(R)·삭제(D)
```

### 🔑 핵심 코드 ① — 관리자 이중 차단 (PDF 패턴)
```python
@app.route('/admin/')
def admin():
    if 'user_id' not in session:       # ① 로그인 안 했으면
        return redirect('/login/')
    if session.get('role') != 'admin': # ② 관리자 아니면 (주소 직접 입력 방어)
        return redirect('/')
    ...
```
> 일반 회원이 주소창에 `/admin/` 쳐도 메인으로 튕김. (검증 완료)

### 🔑 핵심 코드 ② — 기구 CRUD
```python
# CREATE: 등록 후 QR URL도 자동 세팅
cursor.execute('INSERT INTO machines (name, body_part, min_valid_rom, description) VALUES (?,?,?,?)', ...)
mid = cursor.lastrowid
cursor.execute('UPDATE machines SET qr_url=? WHERE id=?', (f'/machine/{mid}/', mid))

# UPDATE: 수정 폼(GET)에 기존값 채우고, 저장(POST)은 UPDATE
cursor.execute('UPDATE machines SET name=?, body_part=?, min_valid_rom=?, description=? WHERE id=?', ...)

# DELETE
cursor.execute('DELETE FROM machines WHERE id=?', (id,))
```

### 🔑 핵심 코드 ③ — 권한 토글 / 본인 삭제 방지
```python
# user면 admin으로, admin이면 user로 (반대로 뒤집기)
new_role = 'admin' if row[0] == 'user' else 'user'

# 자기 자신은 삭제 금지 (삭제하면 본인 로그인이 깨지므로)
if id == session['user_id']:
    return redirect('/admin/users/')
```

### 🔑 핵심 코드 ④ — 인기 기구 통계 (LEFT JOIN + GROUP BY)
```python
cursor.execute('''SELECT m.name, m.body_part, COUNT(ws.id) AS cnt
                  FROM machines m
                  LEFT JOIN workout_session ws ON ws.machine_id = m.id
                  GROUP BY m.id ORDER BY cnt DESC LIMIT 5''')
```
> 운동 기록이 없는 기구도 0회로 표시되도록 LEFT JOIN 사용.

### 🎨 화면 (templates/admin/)
| 파일 | 내용 |
|------|------|
| admin_base.html | 관리자 공통 레이아웃 (상단바 + 메뉴탭) |
| dashboard.html | 통계 지표 5개 + 인기 기구 TOP5 |
| users.html | 회원 목록 + 권한변경/삭제 |
| machines.html | 기구 등록폼 + 목록(수정/삭제) |
| machine_edit.html | 기구 수정 폼 |
| coupons.html | 쿠폰 등록폼 + 목록(삭제) + 지급횟수 |

### ✔️ 검증 (10개 케이스 통과)
| 테스트 | 결과 |
|--------|------|
| 일반회원 /admin/ 차단 | ✅ 302 |
| 통계 대시보드 | ✅ |
| 회원 조회 | ✅ |
| 권한 변경(승급) | ✅ |
| 기구 등록(CREATE) + QR자동 | ✅ |
| 기구 수정(UPDATE) | ✅ |
| 기구 삭제(DELETE) | ✅ |
| 쿠폰 등록(CREATE) | ✅ |
| 쿠폰 삭제(DELETE) | ✅ |
| 본인 삭제 방지 | ✅ |
| 실제 서버 에러 없음 | ✅ |

---

## 🎉 모든 기능 완성!
회원 → 운동(QR) → 기록 → 추천 → 보상 + **관리자(CRUD)** 까지 전부 동작.

## 🗓️ 다음 단계 예정
- **마무리**: 배포(PythonAnywhere) + README + AI 활용 보고서 + 발표 리허설
- **4단계**: 건강 진단 입력 → 추천 루틴 + 식단
- **5단계**: 루틴 완료 → 보상 쿠폰 지급
- **관리자**: 기구/쿠폰/회원 관리 + 통계
- **마무리**: 디자인 다듬기 + 배포(PythonAnywhere) + 발표 리허설

---

## 🔐 기본 계정 정보
| 구분 | 아이디 | 비번 |
|------|--------|------|
| 관리자 | admin | 1234 |
| (테스트용 회원은 직접 가입) | | |
